<?php include 'header.php'; ?>
<div class="container">
    <h1>DECLARAÇÃO SIMPLES BASE</h1>
    <p>Declaramos que <strong>{{ nome }}</strong>, CPF nº <strong>{{ cpf }}</strong>, integra o quadro de pessoal da
        Advocacia-Geral da União, CNPJ 26.994.558/0003-95, encontrando-se investido no cargo efetivo de
        <strong>{{ cargo }}</strong>, sob a matrícula SIAPE nº <strong>{{ matriculaSiape }}</strong>. Ingressou no
        serviço
        público em <strong>{{ dataIngresso }}</strong> e passou a integrar o quadro em <strong>{{ dataQuadro }}</strong>,
        somando até a presente data o total de <strong>{{ totalDias }}</strong> dias brutos, ou seja,
        <strong>{{ totalAnos }}</strong> anos, <strong>{{ totalMeses }}</strong> meses e
        <strong>{{ totalDiasRestantes }}</strong> dias de serviço público, e desempenha suas atribuições funcionais
        perante
        a <strong>{{ localTrabalho }}</strong>.</p>
    <p>Brasília, 9 de abril de 2024.</p>
    <p>(documento assinado eletronicamente)</p>
    <p>BERNARDO BARCA GONÇALVES TEIXEIRA ARANTES<br>Administrador</p>
</div>
<?php include 'footer.php'; ?>